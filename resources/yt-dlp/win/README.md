# yt-dlp binary placeholder

Place `yt-dlp.exe` here for local Bilibili Provider parsing:

```text
resources/yt-dlp/win/yt-dlp.exe
```

The executable is not committed to Git. Prepare it with:

```powershell
powershell -ExecutionPolicy Bypass -File scripts/prepare-ytdlp.ps1
```
